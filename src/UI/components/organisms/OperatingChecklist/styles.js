import { colors } from 'UI/components/molecules/ProductCard/node_modules/UI/res';

const listSelectedBackground = colors.softBack;

export const styles = {
  listItemIcon: {
    minWidth: 40
  },
  checklistInDrawer: {
    backgroundColor: listSelectedBackground
  }
};
